1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
  echo "http(s)://{{ (index .Values.ingress.hosts 0).host }}"
{{- else if eq .Values.service.type "LoadBalancer" }}
  kubectl get svc {{ include "log-processing-api.fullname" . }} -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'
{{- else }}
  kubectl port-forward svc/{{ include "log-processing-api.fullname" . }} 8080:{{ .Values.service.port }}
  echo "http://127.0.0.1:8080"
{{- end }}

